# Briefings

Weekly briefings generated by `/brief` are saved here. Each one is a snapshot of your vault at that point in time.

Claude checks this folder at the start of every session. If it's been 7+ days since the last briefing, Claude generates one automatically.

**Don't delete old briefings** — they're a timeline of how your thinking evolved.
